# Server Configuration
NODE_ENV=development
PORT=5000
HOST=0.0.0.0

# Database Configuration (Local Development PostgreSQL)
# Uncomment the line below if you have a local PostgreSQL instance
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wandstudios

# In production, these will be automatically provided by the hosting environment
# Do not commit the actual .env file with real credentials to version control